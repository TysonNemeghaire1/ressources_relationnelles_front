import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ressources_relationnelles',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
